import styled from 'styled-components';

export const Frame = styled.label``;

export const Label = styled.div`
  font-weight: 500;
  margin-bottom: ${Math.pow(1.414, -3)}rem;
`;

export const Field = styled.div``;
